import {
  Controls,
  Meta,
  Primary,
  Stories,
  Title,
} from "@storybook/addon-docs/blocks";

import * as EmptyStateStories from "./EmptyState.stories";

<Meta of={EmptyStateStories} />

<Title />

`EmptyState` digunakan setelah data berhasil dimuat tetapi tidak ada data untuk
ditampilkan.

## Contoh utama

<Primary />

<Controls />

## Penggunaan yang benar

```text
Belum ada dokumen yang diunggah.
Belum ada kegiatan aktif.
Tidak ada peserta yang sesuai dengan filter.
Belum ada jadwal seleksi.
```
