import {
  Controls,
  Meta,
  Primary,
  Stories,
  Title,
} from "@storybook/addon-docs/blocks";

import * as SideNavigationStories from "./SideNavigation.stories";

<Meta of={SideNavigationStories} />

<Title />

`SideNavigation` digunakan sebagai navigation utama Portal Internal desktop.

Family ini terdiri dari:

```text
SideNavigation
SideNavigationGroup
SideNavigationItem
```
