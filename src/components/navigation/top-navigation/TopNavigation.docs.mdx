import {
  Controls,
  Meta,
  Primary,
  Stories,
  Title,
} from "@storybook/addon-docs/blocks";

import * as TopNavigationStories from "./TopNavigation.stories";

<Meta of={TopNavigationStories} />

<Title />

`TopNavigation` menyediakan navigation horizontal untuk Public Website atau
header portal sederhana.

Family ini terdiri dari:

```text
TopNavigation
TopNavigationItem
```
